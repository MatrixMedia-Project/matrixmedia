[advisories]
# rsa 0.9.10 - Marvin Attack timing sidechannel (RUSTSEC-2023-0071)
# No fix available upstream. Transitive dep from livekit-api -> jsonwebtoken.
# We don't use RSA directly; JWT signing uses HS256 (HMAC).
ignore = ["RUSTSEC-2023-0071"]
